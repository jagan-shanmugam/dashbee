# AI Provider Configuration
# Priority: OpenRouter (if key set) → Gemini (if key set) → AI Gateway (fallback)
#
# IMPORTANT: If deploying to Vercel without AI Gateway billing, you MUST set
# either OPENROUTER_API_KEY or GEMINI_API_KEY to avoid 403 errors.

# Model to use (passed to the selected provider)
# For OpenRouter, use format like: anthropic/claude-haiku-4.5, qwen/qwen3-next-80b-a3b-instruct:free
# For Gemini fallback, this is ignored (uses gemini-2.5-flash automatically)
AI_GATEWAY_MODEL=anthropic/claude-haiku-4.5

# Option 1: OpenRouter (recommended - supports many models including FREE tier)
# Get your key at: https://openrouter.ai/keys
# FREE models available: qwen/qwen3-next-80b-a3b-instruct:free (80B params, excellent tool support)
OPENROUTER_API_KEY=

# Option 2: Gemini (Google AI - good fallback with free tier)
# Get your key at: https://aistudio.google.com/apikey
# Uses gemini-2.5-flash when OpenRouter is not configured
GEMINI_API_KEY=

# OpenAI Configuration (for custom Azure/corporate API gateway)
OPENAI_API_KEY=your-api-key-here
OPENAI_BASE_URL=https://api.openai.com/v1
OPENAI_API_VERSION=2024-10-21

# PostgreSQL Connection (USE READ-ONLY USER IN PRODUCTION)
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/demo
DATABASE_SSL=false

# Supabase Connection (for landing page demo)
# Format: postgresql://user:password@host:port/database
SUPABASE_CONNECTION_STRING=postgresql://postgres:password@db.your-project.supabase.co:5432/postgres

# MySQL Connection (optional)
MYSQL_HOST=localhost
MYSQL_PORT=3306
MYSQL_DATABASE=demo
MYSQL_USER=mysql
MYSQL_PASSWORD=mysql

# SQLite (optional)
SQLITE_PATH=./data/demo.db

# Security Settings
ALLOWED_SCHEMAS=public
ALLOWED_TABLES=
MAX_QUERY_ROWS=1000
QUERY_TIMEOUT_MS=5000

# Cloud Storage - S3 (optional)
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_REGION=us-east-1

# Cloud Storage - GCS (optional)
GCS_PROJECT_ID=
GCS_KEY_FILE=

# Langfuse LLM Observability (optional)
# Sign up at https://langfuse.com to get credentials
# When configured, all LLM calls are traced with latency, token usage, and tool executions
# If not configured, the app works normally without tracing
LANGFUSE_SECRET_KEY=sk-lf-...
LANGFUSE_PUBLIC_KEY=pk-lf-...
LANGFUSE_BASE_URL=https://cloud.langfuse.com
